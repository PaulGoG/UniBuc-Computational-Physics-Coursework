# Manifest.toml is tracked: these are scripts, not a library, and pinning the
# resolved versions is what makes a figure reproducible on another machine.

# Build products of the C/C++ sources
*.o
*.out
a.out

# Generated figures and animations, except those referenced from a README
plots/
output/
